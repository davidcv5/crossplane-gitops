# This is a kustomize patch to replace the KMS key OCID
# Usage: uncomment and update the kmsKeyId value, then add this to patchesStrategicMerge in kustomization.yaml

# apiVersion: certificatesmanagement.oci.upbound.io/v1alpha1
# kind: CertificateAuthority
# metadata:
#   name: test-certificate-authority
# spec:
#   forProvider:
#     kmsKeyId: "ocid1.key.oc1.iad.XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"  # Replace with your actual KMS key OCID